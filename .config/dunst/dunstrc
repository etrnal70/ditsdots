[global]
    ### Display ###
    monitor = 0
    follow = keyboard
    # Dimension
    width = 350
    height = 120
    origin = top-right
    offset = 20x20
    # Progress Bar
    progress_bar = true
    progress_bar_height = 10
    progress_bar_frame_width = 2
    progress_bar_min_width = 0
    progress_bar_max_width = 320
    # Padding
    padding = 14
    horizontal_padding = 14
    text_icon_padding = 18
    # Other
    frame_width = 0
    frame_color = "#616161"
    separator_height = 8
    separator_color = "#00000000"
    notification_limit = 3
    indicate_hidden = true
    sort = true
    idle_threshold = 3m

    ### Text ###
    font = "JetBrainsMono Nerd Font" 10
    line_height = 0
    markup = full
    alignment = left
    vertical_alignment = center
    show_age_threshold = 5m
    word_wrap = true
    ellipsize = middle
    ignore_newline = false
    stack_duplicates = true
    hide_duplicate_count = false
    show_indicators = true

    ### Icons ###
    icon_position = left
    enable_recursive_icon_lookup = true
    icon_theme = "Papirus, Papirus-Dark"
    icon_size = 64
    min_icon_size = 64 # Mark for upcoming breaking changes
    max_icon_size = 64 # Mark for upcoming breaking changes

    ### History ###
    history_length = 10

    ### External ###
    dmenu = "/usr/bin/wofi -d dunst"
    browser = "/usr/bin/xdg-open"
    corner_radius = 5
    ignore_dbusclose = false

    ### Wayland ###
    layer = overlay

    ### Mouse ###
    mouse_left_click = close_current
    mouse_middle_click = close_all
    mouse_right_click = do_action, close_current

[urgency_low]
    background = "#616161"
    foreground = "#FFFFFF"
    highlight = "#FFFFFF"
    timeout = 6

[urgency_normal]
    background = "#616161"
    foreground = "#FFFFFF"
    highlight = "#FFFFFF"
    timeout = 8

[urgency_critical]
    background = "#FFFFFF"
    foreground = "#000000"
    frame_color = "#FF0000"
    timeout = 0

# Custom Rules
[fullscreen_delay_everything]
    fullscreen = pushback
[fullscreen_show_critical]
    msg_urgency = critical
    fullscreen = show

# TODO: Extend custom rules for e.g. battery, volume, etc

# vim: ft=cfg
